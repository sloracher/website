source "https://rubygems.org"

# Use GitHub Pages’ pinned set of gems (includes Jekyll + approved plugins)
gem "github-pages", group: :jekyll_plugins

# Local-only helpers (safe to keep)
gem "tzinfo-data", platforms: [:mingw, :mswin, :x64_mingw]
gem "wdm", "~> 0.1.0", platforms: [:mingw, :mswin, :x64_mingw]

# Optional: explicitly list only plugins that are ALSO allowed by GitHub Pages.
# (These are already pulled in by 'github-pages', but keeping them is OK.)
gem "jekyll-sitemap"
gem "jekyll-gist"
gem "jemoji"
gem "jekyll-include-cache"
gem "jekyll-feed"
